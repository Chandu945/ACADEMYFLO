# PlayConnect Mobile — Environment Configuration
# Copy this file to .env and fill in actual values.
# NEVER commit .env files with real secrets.

# Application
APP_ENV=development
TZ=Asia/Kolkata

# API
API_BASE_URL=http://localhost:3001

# Build flavor (dev | staging | prod)
BUILD_FLAVOR=dev
