{
  "name": "@playconnect/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared type contracts for Academyflo (API envelopes, pagination, roles, constants)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for contracts (types only)'"
  },
  "devDependencies": {
    "@playconnect/eslint-config": "*",
    "@playconnect/tsconfig": "*",
    "eslint": "^8.57.1",
    "typescript": "^5.7.3"
  }
}
