{
  "name": "@playconnect/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configuration for Academyflo",
  "files": [
    "tsconfig.base.json"
  ],
  "scripts": {
    "lint": "echo 'No lint target for tsconfig'",
    "typecheck": "echo 'No typecheck target for tsconfig'",
    "test": "echo 'No test target for tsconfig'"
  }
}
